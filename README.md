### Requirements:

```
* nodejs = v18.16.0
* npm = v9.5.1
* yarn = v1.22.19
```

### Core Dependencies:

```
* "axios": "^1.5.1"                         // Library for making HTTP requests
* "lodash": "^4.17.21"                      // Utility library for JavaScript
* "react": "^18.2.0"                        // Core React library
* "react-dom": "^18.2.0"                    // React library for DOM rendering
* "cross-env": "^7.0.3"                     // Set environment variables across different platforms
```

### UI Libraries:

```
* "@emotion/react": "^11.11.0"              // CSS-in-JS library for styling React components
* "@emotion/styled": "^11.11.0"             // Styled-components library for Emotion
* "@mui/icons-material": "^5.11.16"         // Material UI icons
* "@mui/material": "^5.13.0"                // Material UI components
* "material-ui-confirm": "^3.0.9"           // Confirmation dialog for Material UI
* "react-toastify": "^9.1.3"                // Library for notifications
```

### Drag and Drop:
```
* "@dnd-kit/core": "^6.0.8"                 // Core library for drag and drop
* "@dnd-kit/sortable": "^7.0.2"             // Sortable extension for drag and drop
* "@dnd-kit/utilities": "^3.2.1"            // Utilities for drag and drop
```

### Vite Plugins:

```
* "vite-plugin-svgr": "^3.2.0"              // Vite plugin for importing SVGs as React components
```

### Development Tools:

```
* "eslint": "^8.38.0"                       // Linting tool to maintain code quality and style
* "eslint-plugin-react": "^7.32.2"          // ESLint plugin for React specific linting rules
* "eslint-plugin-react-hooks": "^4.6.0"     // ESLint plugin for React hooks specific linting rules
* "eslint-plugin-react-refresh": "^0.3.4"   // ESLint plugin for React Fast Refresh
```

### TypeScript:

```
* "@types/react": "^18.0.28"                // TypeScript definitions for React
* "@types/react-dom": "^18.0.11"            // TypeScript definitions for ReactDOM
```

### Vite:

```
* "vite": "^4.3.2"                          // Next generation frontend tooling
* "@vitejs/plugin-react-swc": "^3.0.0"      // Vite plugin for React with SWC (Speedy Web Compiler)
```